{
  "name": "monastyr-backend",
  "version": "1.0.0",
  "build": {
    "command": "npm install --legacy-peer-deps && npx prisma generate && npm run build",
    "output": "dist"
  },
  "start": {
    "command": "npm start"
  },
  "env": {
    "NODE_ENV": "production",
    "PORT": "3000"
  }
}
