{
  "name": "monastyr-backend",
  "version": "1.0.0",
  "build": {
    "command": "cd backend && npm install --legacy-peer-deps && npx prisma generate && npm run build",
    "output": "backend/dist"
  },
  "start": {
    "command": "cd backend && npm start"
  },
  "env": {
    "NODE_ENV": "production",
    "PORT": "3000"
  }
}
